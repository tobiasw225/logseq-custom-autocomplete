export declare const messageType = "application/x-postmate-v1+json";
export declare const defaultRequestTimeout = 10000;
export declare const maxHandshakeRequests = 5;
export declare const sanitize: (message: any, allowedOrigin: any) => boolean;
export declare const resolveValue: (model: any, property: string, args: Array<any>) => Promise<any>;
/**
 * Composes an API to be used by the parent
 * @param {Object} info Information on the consumer
 */
export declare class ParentAPI {
    parent: Window;
    frame: HTMLIFrameElement;
    child: Window;
    events: {};
    childOrigin: string;
    listener: (e: any) => void;
    private readonly messagePort?;
    private addTransportListener;
    private removeTransportListener;
    private postToChild;
    constructor(info: Postmate);
    get(property: string, ...args: any): Promise<unknown>;
    call(property: string, data: any): void;
    on(eventName: string, callback: Function): void;
    destroy(): void;
}
export declare class ChildAPI {
    private readonly model;
    private parent;
    private readonly parentOrigin;
    private child;
    private messagePort?;
    private readonly listener;
    private addTransportListener;
    private postToParent;
    constructor(info: Model);
    emit(name: string, data: any): void;
}
export type PostMateOptions = {
    container: HTMLElement;
    url: string;
    id?: string;
    classListArray?: Array<string>;
    name?: string;
    model?: any;
    allow?: string;
    /**
     * Prefer using MessageChannel/MessagePort after handshake.
     * Defaults to false to keep backward-compatible behavior with older SDKs.
     */
    enableMessageChannel?: boolean;
};
/**
 * The entry point of the Parent.
 */
export declare class Postmate {
    static debug: boolean;
    static requestTimeout: number;
    container?: HTMLElement;
    parent: Window;
    frame: HTMLIFrameElement;
    child?: Window;
    childOrigin?: string;
    url: string;
    model: any;
    static Model: any;
    messagePort?: MessagePort;
    private readonly enableMessageChannel;
    constructor(opts: PostMateOptions);
    sendHandshake(url?: string): Promise<unknown>;
    destroy(): void;
}
/**
 * The entry point of the Child
 */
export declare class Model {
    child: Window;
    model: any;
    parent: Window;
    parentOrigin: string;
    messagePort?: MessagePort;
    private enableMessageChannel;
    constructor(model: any);
    sendHandshakeReply(): Promise<unknown>;
}
